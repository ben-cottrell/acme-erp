apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-management-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-management-api
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: domain-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-management-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inventory-management-api
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: domain-api
    spec:
      containers:
        - name: inventory-management-api
          image: acme-erp/inventory-management-api
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__DomainDatabase
              valueFrom:
                secretKeyRef:
                  name: erp-sqlserver-connection-strings
                  key: inventory
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-management-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-management-api
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inventory-management-api
  ports:
    - name: http
      port: 80
      targetPort: http
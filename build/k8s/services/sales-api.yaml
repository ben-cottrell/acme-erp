apiVersion: apps/v1
kind: Deployment
metadata:
  name: sales-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: sales-api
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sales-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sales-api
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: sales-api
          image: acme-erp/sales-api
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__ModuleDatabase
              valueFrom:
                secretKeyRef:
                  name: erp-sqlserver-connection-strings
                  key: sales
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: sales-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: sales-api
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sales-api
  ports:
    - name: http
      port: 80
      targetPort: http

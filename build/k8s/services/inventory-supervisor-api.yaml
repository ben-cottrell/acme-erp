apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-supervisor-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-supervisor-api
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: application-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-supervisor-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inventory-supervisor-api
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: application-api
    spec:
      containers:
        - name: inventory-supervisor-api
          image: acme-erp/inventory-supervisor-api
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: DomainApis__InventoryManagement
              value: http://inventory-management-api/domain/inventory/api
            - name: DomainApis__Purchasing
              value: http://purchasing-api/domain/purchasing/api
            - name: DomainApis__OrderFulfilment
              value: http://order-fulfilment-api/domain/fulfilment/api
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-supervisor-api
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-supervisor-api
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inventory-supervisor-api
  ports:
    - name: http
      port: 80
      targetPort: http
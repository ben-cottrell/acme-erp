apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-management-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-management-ui
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-management-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inventory-management-ui
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: ui
    spec:
      containers:
        - name: inventory-management-ui
          image: acme-erp/inventory-management-ui
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-management-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: inventory-management-ui
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inventory-management-ui
  ports:
    - name: http
      port: 80
      targetPort: http

apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-administration-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: security-administration-ui
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: application-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: security-administration-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: security-administration-ui
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: application-ui
    spec:
      containers:
        - name: security-administration-ui
          image: acme-erp/security-administration-ui
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ApplicationApi__BaseUrl
              value: http://security-administration-api/apps/security-administration/api
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: security-administration-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: security-administration-ui
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: security-administration-ui
  ports:
    - name: http
      port: 80
      targetPort: http
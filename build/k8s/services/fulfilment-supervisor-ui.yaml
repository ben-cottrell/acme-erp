apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfilment-supervisor-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: fulfilment-supervisor-ui
    app.kubernetes.io/part-of: acme-erp
    app.kubernetes.io/component: application-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fulfilment-supervisor-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fulfilment-supervisor-ui
        app.kubernetes.io/part-of: acme-erp
        app.kubernetes.io/component: application-ui
    spec:
      containers:
        - name: fulfilment-supervisor-ui
          image: acme-erp/fulfilment-supervisor-ui
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ApplicationApi__BaseUrl
              value: http://fulfilment-supervisor-api/apps/fulfilment-supervisor/api
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: fulfilment-supervisor-ui
  namespace: erp-local
  labels:
    app.kubernetes.io/name: fulfilment-supervisor-ui
    app.kubernetes.io/part-of: acme-erp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fulfilment-supervisor-ui
  ports:
    - name: http
      port: 80
      targetPort: http